import React from 'react';
import Location from './Location';
import WeatherData from './WeatherData';

const WeatherLocation = () => (
    <div> 
        WeatherLocation (Aquaponics App)
        <Location></Location>
        <WeatherData></WeatherData>
    </div>
);

export default WeatherLocation;